#include "workspace.xcconfig"

HEADER_SEARCH_PATHS = $(inherited) $(LIB_INCLUDES) $(BOOST_INCLUDES) $(JSON_INCLUDES) $(LIBPNG_INCLUDES) $(ELPP_INCLUDES) $(ZLIB_INCLUDES) $(LIBATLAS2D_INCLUDES)

LIBRARY_SEARCH_PATHS = $(inherited) $(BOOST_LIBS) $(LIBPNG_LIBS)

OTHER_LDFLAGS = $(inherited)
